# Next To Do

这里只维护当前最值得推进且可以验证的下一步。

## 当前

1. **让 ZCode 只执行 Phase 0（当前阻塞）**
   - 输入：PRD、技术规范、前端视觉方案的本地文件或可访问内容。
   - 动作：使用 `tasks/zcode-execution-plan.md` 的 Phase 0 提示词，登记文件路径、版本和日期，确认当前基线；不初始化工程、不安装依赖、不写业务代码。
   - 验证：三类资料均能读取，T-001 完成，且不存在无法判断的新旧版本冲突。

2. **形成一页实施基线**
   - 输入：已确认的三类源资料。
   - 动作：明确目标用户、核心场景、成功指标、MVP、非目标、技术边界和冲突裁决人。
   - 验证：团队能用同一句话说明“为谁解决什么问题、第一版怎样算成功”；关键冲突均有负责人。

3. **建立追踪矩阵并选择第一条垂直切片**
   - 输入：实施基线与已解决的 P0 问题。
   - 动作：映射“需求—技术模块—视觉状态—任务—验收证据”，再选择一个最小端到端用户闭环作为第一批实现。
   - 验证：核心需求无孤立项；垂直切片包含输入、处理、输出和失败路径；每个任务都有来源、依赖和验证方式。

## 暂不执行

- 在源资料缺失时猜测产品功能、技术栈或页面清单。
- 未经确认向外部任务系统同步或发布内容。
